package io.github.yozedens.secureauth

import android.app.Application

/** Hosts the manual dependency container (design §48), added in later milestones. */
class SecureAuthApplication : Application()
